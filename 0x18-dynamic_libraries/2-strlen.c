#include "main.h"

/**
 * _strlen - Calculate the length of a null-terminated string
 *
 * @s: for Input
 *
 * Return: return the result as an integer
 */

int _strlen(char *s)
{
	int j;

	for (j = 0; s[j] != '\0'; j++)
	{
		continue;
	}
	return (j);
}
